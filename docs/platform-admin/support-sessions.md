# Support Sessions

Support sessions are platform-admin only, tenant-scoped, reason-coded, and
time-bound. The API rejects open-ended sessions and sessions for churned,
pending-deletion, or deleted tenants.

Support ticket assignment requires an active platform user. Closing a ticket
requires a resolution summary, which is captured in the audit entry.
